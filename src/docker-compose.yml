version: '3.4'

services:
  catalogdb:
    image: mongo
    volumes:
      - catalogdb:/data/db 

  # catalog.api:
  #   image: ${DOCKER_REGISTRY-}catalogapi
  #   build:
  #     context: .
  #     dockerfile: Services/Catalog/Catalog.Api/Dockerfile

volumes:
  catalogdb: